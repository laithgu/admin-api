# Solid Cable 用主库存广播消息 —— dev / prod 都跨进程，
# bin/jobs 进程 broadcast 的消息 puma 进程能收到（async adapter 做不到这点）
default: &default
  adapter: solid_cable
  polling_interval: 0.1.seconds
  message_retention: 1.day

development:
  <<: *default

test:
  adapter: test

production:
  <<: *default
